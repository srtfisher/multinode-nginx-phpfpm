#!/bin/sh
#
# Master Manager Command
# Controls the master node of the application

BIN_BASE=$(dirname $0)
source BIN_BASE/../var/common.sh

case "$1" in
  "create-site")
    
    ;;

  "delete-site")
    
    ;;

  "deploy-site")
    
    ;;

  *)
    echo "Usage: master-manager {create-site|delete-site|deploy-site|status}"
    exit 1
    ;;
esac

exit 0
  